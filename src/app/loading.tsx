'use client';

import LoadingAnimatedIcon from '@/components/LoadingAnimatedIcon';

export default function Loading() {
	return (
		<div className="mt-[-5rem] flex items-center justify-center p-64">
			<LoadingAnimatedIcon />
		</div>
	);
}
